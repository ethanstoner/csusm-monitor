services:
  monitor:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - monitor-data:/app/data
    deploy:
      resources:
        limits:
          memory: 2g
          cpus: "2.0"
    restart: unless-stopped

volumes:
  monitor-data:
