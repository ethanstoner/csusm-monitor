# For local development (without Docker)
MQTT_HOST=localhost
MQTT_PORT=1883
FRIGATE_HOST=localhost
FRIGATE_PORT=5000

# When running via docker-compose, these are set automatically
# in the compose file (MQTT_HOST=mosquitto, FRIGATE_HOST=frigate)
